apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: privatebin
spec:
  parentRefs:
    - name: external-gateway
      namespace: default
  hostnames:
    - paste.blyatflix.com
  rules:
    - backendRefs:
        - name: privatebin
          port: 8080
